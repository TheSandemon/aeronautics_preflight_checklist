org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
# NOTE: Do not commit machine-specific paths (e.g. org.gradle.java.home).
# Set those in your local ~/.gradle/gradle.properties instead.

# Mod details
mod_id=aeronautics_preflight_checklist
mod_name=Aeronautics Preflight Checklist
mod_license=MIT
mod_version=1.0.1
mod_group_id=com.ladderstar.apc
mod_authors=ladderstar
mod_description=Expands the Diagram item from Create: Aeronautics with an expandable preflight checklist, checking mass, lift, balance, thrust, blocked parts, missing essentials, and cargo instability.

# Version dependencies
minecraft_version=1.21.1
minecraft_version_range=[1.21.1,1.22)
neo_version=21.1.65
neo_version_range=[21.1.65,)
loader_version_range=[1,)

# CurseForge / Modrinth publishing
# DO NOT commit real keys here. Use environment variables or your local
# ~/.gradle/gradle.properties instead.
curseforge_project_id=1564032
modrinth_project_id=l2LYH0FN